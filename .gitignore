# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Media (local files — never commit)
media/
*.mp4
*.mkv
*.avi

# Progress DB (legacy global paths + per-user dirs)
data/progress.json
data/completed.json
data/manga_completed.json
data/manga_page_progress.json
data/users.json
data/users/
data/.accounts_migrated
data/requests.json
data/recommendations.json

# Grab URL cache / overrides (local)
data/grab/cache.json
data/grab/overrides.json

# Local asset registry (fetched streams)
data/local_registry.json

# Optional flat asset dump used by stream_fetch CLI
media_assets/

# MAL OAuth tokens + local sync/cache (local only)
data/mal/tokens.json
data/mal/pending_oauth.json
data/mal/last_sync.json
data/mal/users/
data/mal/cache/

# AniList / other API caches
data/cache/

# Personal catalog selection
data/catalog/selected.json

# MAL manga catalog + cache
data/manga/selected.json
data/mal/manga_cache/

# MangaDex chapter-title cache (local sync)
data/mangadex/

# Sync skip index (per-install completion flags)
data/sync_index/

# IDE
.vscode/
!.vscode/extensions.json

# OS
.DS_Store
Thumbs.db

.env
.env.*
!.env.example
data/.flask_secret

# Local data backups (scripts/backup_data.py)
backups/
